using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraControllerXbox : MonoBehaviour {
	/*
	public float 
	X,Y,
	speedX,speedY;

	Vector3 PlayerV;

	GameObject Player;

	void Start() {
		Player = GameObject.Find ("Player");
	}

	void Update(){
		X = Input.GetAxis("Joy X") * Time.deltaTime * speedX;
		Y = Input.GetAxis ("Joy Y") * Time.deltaTime * speedY;

		//PlayerV = Player.transform.eulerAngles;

		Player.transform.Rotate (X,0,0);
	}*/
}
